[phases.setup]
nixPkgs = ["tesseract", "python311"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "python -m playwright install chromium --with-deps" 
]

[start]
cmd = "python main.py"