[phases.setup]
nixPkgs = ["python311", "tesseract"]

[phases.install]
cmds = [
    "pip install -r requirements.txt"
]

[start]
cmd = "python -m playwright install chromium --with-deps && python main.py"